# Clerk publishable key — enables real OAuth (Google / Apple / Spotify) on /signin.
# Get it from the Clerk dashboard (Publishable key). Leave unset to use the
# UI-only mock sign-in (Siri-orb welcome) while building without a backend.
VITE_CLERK_PUBLISHABLE_KEY=
