// Shared "Lemni Light" auth design tokens — depth is shadow-driven, no borders.
// Used across the sign-in and OTP verification screens so they stay identical.
export const CTA_SHADOW = 'rgba(0,0,0,0.4) 0px 2px 5px 0px'
export const OAUTH_SHADOW =
  'rgba(95,122,143,0.25) 0px 1px 2px 0px, rgb(255,255,255) 1px 1px 1px 0px inset'
export const INPUT_SHADOW =
  'rgba(95,122,143,0.23) 0px 2px 3px 0px, rgba(0,0,0,0.03) -1px -1px 1px 0px inset, rgb(255,255,255) 1px 1px 1px 0px inset'

// Display serif used for auth headings (documented design-system exception).
export const SERIF = "'Instrument Serif', Georgia, serif"
